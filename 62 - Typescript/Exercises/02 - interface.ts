// Create an interface called Person with the following properties:

// name of type string
// age of type number
// email of type string

interface Person{
    nome: string;
    idade: number;
    email: string;
}